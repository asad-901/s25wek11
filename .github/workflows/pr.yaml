name: pr-validate-wf
on: 
   pull_request:
     types:
       - opened
       - synchronize
   push:
      branches:
        - develop

jobs:
    pr-validate-job:
      runs-on: ubuntu-latest
      steps:
        - name: welcome steps
          run: echo "Welcome to actions"
        - name: second steps
          run: echo "Welcome Again" 
    second-jobs:
      runs-on: ubuntu-latest
      steps:
        - name: first job 2
          run: echo "Welcome Again Again"        
